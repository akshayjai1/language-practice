tring str=s;
    //      while (str[i] >= '0' && str[i] <= '9') {
		// 			 cout << i<<endl;
		// 			 if (base > INT_MAX / 10 || (base == INT_MAX / 10 && str[i] - '0' > 7))
		// 			 {
		// 				 if (sign == 1)
		// 					 return INT_MAX;
		// 				 else
		// 					 return INT_MIN;
		// 			 }
		// 		base  = 10 * base + (str[i++] - '0');
    // }